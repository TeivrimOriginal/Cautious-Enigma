[package]
name = "linguarust-desktop"
version = "0.2.0"
edition = "2024"
description = "Desktop shell for LinguaRust"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tao = "0.34"
wry = "0.53"

[profile.release]
strip = true
